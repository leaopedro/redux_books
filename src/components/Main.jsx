import React from 'react';
import SearchContainer from '../containers/SearchContainer';

const Main = ({ title }) => {
  return (
    <div>
      <SearchContainer />
    </div>
  );
};


export default Main;
